name: Update Amundi Fund Prices

on:
  schedule:
    - cron: '0 4,7,10,13,16,19 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fetch NAV history
        run: |
          curl -s -X POST "https://funds.amundi-ee.com/product-services/fdr/share/v3/full/2743661-4991185" \
            -H "Content-Type: application/json" \
            -A "Mozilla/5.0" \
            --data '{"fields":["navHistory"]}' \
            -o amundi-canalplus-e.json

          curl -s -X POST "https://funds.amundi-ee.com/product-services/fdr/share/v3/full/2743661-2762280" \
            -H "Content-Type: application/json" \
            -A "Mozilla/5.0" \
            --data '{"fields":["navHistory"]}' \
            -o amundi-havas-e.json

          curl -s -X POST "https://funds.amundi-ee.com/product-services/fdr/share/v3/full/2743661-5735181" \
            -H "Content-Type: application/json" \
            -A "Mozilla/5.0" \
            --data '{"fields":["navHistory"]}' \
            -o amundi-lhg-e.json

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add amundi-canalplus-e.json amundi-havas-e.json amundi-lhg-e.json
          git diff --staged --quiet || git commit -m "Update Amundi fund prices"
          git push
